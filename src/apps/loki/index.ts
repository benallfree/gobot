import { AppInfo } from '../'

export const loki: AppInfo = {
  name: `Loki`,
  description: `Like Prometheus, but for logs.`,
  homepage: `https://grafana.com/loki`,
  isAlpha: true,
  factory: `grafana/loki`,
}
