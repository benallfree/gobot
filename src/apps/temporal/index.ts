import type { AppInfo } from '../'

export const temporal: AppInfo = {
  name: 'Temporal',
  description: `Temporal service`,
  homepage: `https://docs.temporal.io`,
  isAlpha: true,
  factory: `temporalio/temporal`,
}
