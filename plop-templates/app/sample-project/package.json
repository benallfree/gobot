{
  "name": "{{lowerCase slug}}-gobot-sample",
  "bin": {
    "{{slug}}-gobot-sample": "./index.mjs"
  },
  "scripts": {
    "dev": "./index.mjs",
    "{{slug}}": "{{slug}}"
  },
  "dependencies": {
    "gobot-{{lowerCase slug}}": "{{version}}"
  }
}
